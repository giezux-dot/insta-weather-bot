- name: Checkout code
        uses: actions/checkout@v4.1.7 # Sprawdź, czy jest dostępna v5, jeśli nie, v4.1.7+ zazwyczaj rozwiązuje problem

      - name: Set up Python
        uses: actions/setup-python@v5.1.0 # Wersja 5.1.0+ oficjalnie wspiera Node 24
        with:
          python-version: '3.12'
